const { default: mongoose } = require("mongoose");
const db = require("../models");
const { user: User, role: Role, bookSet: BookSet } = db;

const BookSetController = {};
module.exports = BookSetController;
